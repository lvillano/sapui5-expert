_schema-version: "3.2"
ID: invoices3
version: 0.0.1
modules:
- name: invoices3-approuter
  type: approuter.nodejs
  path: invoices3-approuter
  requires:
  - name: invoices3_html_repo_runtime
  - name: dest_invoices3
  parameters:
    disk-quota: 256M
    memory: 256M
- name: invoices3_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: invoices3_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sapui5-content.zip
      name: sapui5
      target-path: resources/
- name: sapui5
  type: html5
  path: sapui5
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: invoices3_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: invoices3_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: dest_invoices3
  type: org.cloudfoundry.managed-service
  parameters:
    service: destination
    service-plan: lite
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
